{
  "name": "soyatrack/backend",
  "description": "Lightweight native PHP backend with simple routing, PDO, JWT auth, and basic security.",
  "type": "project",
  "require": {
    "php": ">=8.0",
    "ext-json": "*"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  },
  "require-dev": {
    "phpunit/phpunit": "^10.0"
  },
  "scripts": {
    "test": "phpunit --colors=always"
  }
}
